x-common:
  database: &db-environment
    # Do not remove the "&db-password" from the end of the line below, it is important
    # for GEBilling functionality.
    MYSQL_PASSWORD: &db-password "CHANGE_ME"
    MYSQL_ROOT_PASSWORD: "CHANGE_ME_TOO"

#
# ------------------------------------------------------------------------------------------
# DANGER ZONE BELOW
#
# The remainder of this file likely does not need to be changed. Please only make modifications
# below if you understand what you are doing.
#
services:
  database:
    image: mariadb:lts
    restart: always
    command: --default-authentication-plugin=mysql_native_password
    volumes:
      - "./database:/var/lib/mysql"
    environment:
      <<: *db-environment
      MYSQL_DATABASE: "gebilling"
      MYSQL_USER: "gebilling"
    networks:
      - gebilling_nw
  cache:
    image: redis:alpine
    restart: always
    networks:
      - gebilling_nw
  gebilling:
    image: ghcr.io/godseyeglobal/gebilling:latest
    restart: always
    ports:
      - "80:80"
    volumes:
      - "./:/app/var/"
      - "./storage/logs:/app/storage/logs"
      - "./storage/public:/app/storage/app/public"
      - "./themes:/app/themes"
      - "./extensions:/app/extensions"
      - "app:/app"
    environment:
      DB_PASSWORD: *db-password
      APP_ENV: "production"
      CACHE_STORE: "redis"
      REDIS_HOST: "cache"
      DB_CONNECTION: "mariadb"
      DB_HOST: "database"
      DB_PORT: "3306"
      # Set to "true" to skip renewal of default themes and extensions
      # By default, default themes and extensions are always renewed on startup
      GEBILLING_SKIP_DEFAULT: "false"
    networks:
      - gebilling_nw
  # Asset Builder Service - used to build frontend assets (themes/extensions)
  asset-builder:
    image: node:22-alpine
    profiles: ["build"]
    working_dir: /app
    volumes:
      - "./themes:/app/themes"
      - "./extensions:/app/extensions"
      - "./:/app/var"
      - "app:/app"
    command: >
      sh -c "echo 'Asset Builder ready for npm commands' &&
      echo 'Working directory: /app' &&
      echo 'Usage: docker compose run --rm asset-builder npm install && npm run build' &&
      tail -f /dev/null"
    networks:
      - gebilling_nw
networks:
  gebilling_nw:
    driver: bridge

volumes:
  app:
